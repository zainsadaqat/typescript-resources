// declaration_type variable_name: data_type = value;

// Type number
let weight: number = 100;
let age = 23; // weight and age are of type number;

// Type string
let myName = 'Zain Sadaqat';
// let myPetName: string = "I don't have any pet";
// myPetName = "I don't have any pet".length;  because .length returns a number
// myPetName = "I don't have any pet".length === 0;  because === returns a boolean

// Type boolean
let isAlive = true;
isAlive = "I don't have any pet".length > 10; 